# .goreleaser.yml
# Build customization
dist: goreleaser-build
builds:
  - main: main.go
    binary: mouthful
    goos:
      - darwin
    goarch:
      - amd64
    hooks:
      pre: ./goreleaser-pre.sh
# Archive customization
archive:
  format: tar.gz
  files:
    - static/*
    - config.json